{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "go_gdb_launch",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            //当运行单个文件时{workspaceFolder}可改为{file}
            "program": "${file}",
            "env": {},
            "args": [],
            "cwd": "${fileDirname}",
            "console": "integratedTerminal"
        }
    ]
}
